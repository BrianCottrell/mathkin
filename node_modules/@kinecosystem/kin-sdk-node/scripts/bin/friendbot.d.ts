import { IAccountDataRetriever } from "./blockchain/accountDataRetriever";
import { Address, TransactionId } from "./types";
export declare class Friendbot {
    private readonly _url;
    private readonly _accountDataRetriever;
    constructor(_url: string, _accountDataRetriever: IAccountDataRetriever);
    createOrFund(address: Address, amount: number): Promise<TransactionId>;
    private parseResponse;
    private fundUrl;
    private createAccountUrl;
}
