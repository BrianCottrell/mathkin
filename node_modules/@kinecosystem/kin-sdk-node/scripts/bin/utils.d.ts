import { Address } from "./types";
export declare namespace Utils {
    function isValidAddress(address: Address): boolean;
    function verifyValidAddressParamAsync(address: Address): Promise<void>;
    function verifyValidAddressParam(address: Address): void;
}
