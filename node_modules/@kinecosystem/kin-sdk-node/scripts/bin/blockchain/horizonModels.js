"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const transactionRetriever_1 = require("./transactionRetriever");
var Transaction;
(function (Transaction) {
    function decodeTransaction(params) {
        return transactionRetriever_1.TransactionRetriever.fromTransactionPayload(params.envelope, params.networkId);
    }
    Transaction.decodeTransaction = decodeTransaction;
    function decodeRawTransaction(params) {
        return transactionRetriever_1.TransactionRetriever.fromTransactionPayload(params.envelope, params.networkId, false);
    }
    Transaction.decodeRawTransaction = decodeRawTransaction;
})(Transaction = exports.Transaction || (exports.Transaction = {}));
//# sourceMappingURL=horizonModels.js.map