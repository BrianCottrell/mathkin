import { Account, Memo, MemoType, Transaction, TransactionBuilder as BaseTransactionBuilder, xdr } from "@kinecosystem/kin-sdk";
import { Channel } from "./channelsPool";
interface TransactionBuilderOptions extends BaseTransactionBuilder.TransactionBuilderOptions {
    fee: number;
    appId: string;
    memo?: Memo<MemoType.Text>;
}
export declare class TransactionBuilder {
    private readonly _transactionBuilder;
    private readonly _channel?;
    private readonly _appId?;
    constructor(sourceAccount: Account, options: TransactionBuilderOptions, channel?: Channel);
    addFee(fee: number): this;
    setTimeout(timeout: number): this;
    addTextMemo(memo: string): void;
    addMemo(memo: Memo): this;
    addOperation(operation: xdr.Operation): this;
    readonly channel: Channel | undefined;
    build(): Transaction;
}
export {};
