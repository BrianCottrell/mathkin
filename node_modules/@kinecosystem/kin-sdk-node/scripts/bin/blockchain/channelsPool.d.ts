import { KeyPair } from "./keyPair";
export declare class ChannelsPool {
    private readonly channels;
    constructor(seeds: string[]);
    private getFreeChannels;
    acquireChannel<T>(func: (channel: Channel) => Promise<T>): Promise<T>;
    readonly status: ChannelsPoolStatus;
    private releaseChannel;
}
export declare type ChannelState = 'busy' | 'free';
export interface Channel {
    readonly keyPair: KeyPair;
    state: ChannelState;
}
export interface ChannelsPoolStatus {
    totalChannels: number;
    freeChannels: number;
    busyChannels: number;
    channels: Channel[];
}
