import { Server } from "@kinecosystem/kin-sdk";
import { Address } from "../types";
import { OnPaymentListener, PaymentListener } from "./horizonModels";
export declare class BlockchainListener {
    private readonly _server;
    constructor(_server: Server);
    createPaymentsListener(onPayment: OnPaymentListener, addresses: Address[]): PaymentListener;
}
