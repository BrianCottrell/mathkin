/// <reference types="node" />
import { Address } from "../types";
export declare class KeyPair {
    private _keypair;
    readonly seed: string;
    readonly publicAddress: Address;
    private constructor();
    sign(data: Buffer): void;
    static fromSeed(seed: string): KeyPair;
    static generate(): KeyPair;
    static generateHDSeed(baseSeed: string, salt: string): KeyPair;
    static addressFromSeed(seed: string): Address;
}
