"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const kin_sdk_1 = require("@kinecosystem/kin-sdk");
const errors_1 = require("./errors");
const config_1 = require("./config");
var Utils;
(function (Utils) {
    function isValidAddress(address) {
        if (address.length !== config_1.ADDRESS_LENGTH) {
            return false;
        }
        return kin_sdk_1.StrKey.isValidEd25519PublicKey(address);
    }
    Utils.isValidAddress = isValidAddress;
    function verifyValidAddressParamAsync(address) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!isValidAddress(address)) {
                throw new errors_1.InvalidAddressError();
            }
        });
    }
    Utils.verifyValidAddressParamAsync = verifyValidAddressParamAsync;
    function verifyValidAddressParam(address) {
        if (!isValidAddress(address)) {
            throw new errors_1.InvalidAddressError();
        }
    }
    Utils.verifyValidAddressParam = verifyValidAddressParam;
})(Utils = exports.Utils || (exports.Utils = {}));
//# sourceMappingURL=utils.js.map